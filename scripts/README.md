# Scripts

Only reusable local/remote execution helpers belong here. Do not place C/C++ source files in this directory.

- `remote-probe.sh`: read-only remote hardware/toolchain probe.
- `remote-run.sh`: sync the project and run on the remote node with a maximum of 32 logical CPUs.
- `check-layout.sh`: verify source-file placement.
