# 优化工作手册 —— agent 的搜索算子

> 本文件与具体赛题无关，讲的是**怎么搜索**。任务定义与红线在 `AGENTS.md`，
> 本项目的现状与流程在 `CLAUDE.md`。

## 0. 为什么有这份文档

一个 agent 在长期优化里最大的损耗，**不是想不出新点子，而是舍不得撤销自己写下的结论**。

在上下文里，「我**测到**的」和「我**判断**的」长得一模一样——都是记录，都用同样确信的
语气。于是"判负"会累积成一堵不可逾越的墙，而墙后面往往就是最大的那块收益。

证据（同一集群上的 trsm 项目，256 条实验）：收尾阶段三次最大的收益**全部是重开旧结论**，
每条旧结论都有一个可诊断的缺陷（混淆项 / 推理错误 / 未审假设）；
同期沿梯度线性扫的九个方向**全部判负**。

**这份文档的目的，是让"关门"这个动作可撤销、可审计、有成本。**

---

## 1. 记录格式：记价格，不记裁决

一条实验记录如果只写「判负 −13%」，那扇门就永久关死了。
要让它几天后还能被有效质疑，必须提供三样**裁决本身不提供**的东西：

1. **两侧的原始数字，不是差值。**「−13%」不能追问，「3054 vs 2660」可以。
2. **测量时的旁置条件**——尤其是"为了测这个，我把什么别的东西关掉了"。
3. **从数字到裁决的推理链，逐段可见**，这样能一段一段攻击。

**第 2 条最致命**：当一个想法必须先破坏另一样东西才能测试时，它测出来的价格里
混着那次破坏的代价。trsm 有一条否定就是这么错的——**价格错了 9 倍**，
真实成本 0.20 ms 被记成了 1.76 ms，一个 +4.6% 的收益被埋了六天。

> **裁决是一个标量，表格是一个模型。你只能从模型里推出不同的结论。**

每条实验**同时**写进 `OPTIMIZATIONS.md`（散文，给人读）和 `LEDGER.tsv`（结构化，给机器读）。

---

## 2. 算子家族与先验

下表是 trsm 项目 256 条实验的**实测命中率**，作为本项目的**初始先验**：

| 算子家族 | 典型动作 | 命中率 | 幅度 |
|---|---|---|---|
| **① 边界一致性 / 对齐 / 整除** | 让分块边界落到 cache line、行块、线程数上；让两个循环的网格一致；留一核给 OS | **~9/15** | 中，但**总量最大** |
| **② 相位消除** | 让某个中间相位不存在，而不是让它变快 | 2/3 | **大** |
| **③ 调度结构替换** | 换分块/递归/遍历结构 | 4/8 | **最大**，代价也最大 |
| **④ 内存放置** | HBM/DDR、页大小、pool 归属 | 1/2 | 中 |
| **⑤ 构建旗标** | 对齐、优化级别、codegen | 1/2 | 小，**成本近零** |
| **⑥ 延迟隐藏** | 软件预取、流水、工作窃取、动态调度、减线程 | **~2/20** | 小 |

**这张表是关于这台机器的可迁移结论：它对"结构"敏感，对"隐藏延迟"几乎免疫。**
zgemm 未必完全一样——但在你自己积累出反例之前，**按这个先验分配机时**。

> **每积累 30 条实验，用 `LEDGER.tsv` 重算一次本项目自己的命中率表，替换掉这张继承来的。**
> 这是把"下一步试什么"从直觉变成 bandit 问题的唯一办法。

---

## 3. 五个探索算子

**纪律：不要连续三轮只做参数扫。每一轮至少动用下面一个。**

### ① 边界一致性枚举 —— 命中率最高，而且是机械的

把「软件边界 × 硬件边界」列成一张表，逐格检查是否一致。

- **软件边界**：每个循环的分块粒度、每个缓冲的起始地址与对齐、每个相位的工作单元数、
  每个线程组的大小、每个数据结构的 tile 网格、每次拷贝的粒度。
- **硬件边界**：cache line、页/大页、L1/L2 容量、线程数、NUMA 节点、系统核、SVL、DRAM 行。

**只要有一格不一致，那里通常就有钱。** trsm 最大的一次收益（+9.7%）就是
**两个循环的 tile 网格互相不一致**，这个不一致存在了好几周，没人枚举过。

这件事 **agent 天生擅长**（不知疲倦地穷举）而**人天生跳过**（觉得太蠢）。第一轮就做。

### ② 重新定价 —— 否定结果有保质期

一条否定只对"它被否定时的基线"有效。基线一变，价格就得重算。

> **保质期判据：当前成绩相对该条否定时的基线变化 > 5%，该否定自动重新入队。**

所以 `LEDGER.tsv` 必须记下**判负时的基线分数**。

### ③ 去混淆

遍历判负列表，标出"一次改了两个变量"的 A/B，**只让一个变量动**，重测。
成本极低、回报极高，但**看起来像重复劳动**，所以从来没人主动做。

### ④ 相位存在性

对每个相位问的不是"怎么让它更快"，而是：
**"它的输出被谁消费？生产者能不能直接写成消费者要的形状？"**

trsm 有一整个打包相位就是这么消失的（不是变快，是不存在了），幅度超过任何一次调参。

### ⑤ 判据审计

定期验证"你正在优化的**代理指标**是否还与目标相关"。trsm **三次推翻了自己的判据**。
**判据错了，命中率就是 0，再多机时也没用。**

做法：找一个**能把两个候选解释分开**的测量，而不是建一个解析模型。

---

## 4. 建模 vs 测量

> **模型是最贵、最容易生成自信错误结论的部件；测量是最便宜、最不会骗人的部件。**

trsm 里建的解析模型（cache 工作集、流量账、发射端口下界）**大部分是错的**，
其中一条错误的"硬地板"结论写进文档误导了好几天。
而每次设计一个**判别性**测量，一次就定案。

判别性测量的例子：
- 把"活多"和"跑得慢"分开 —— 工作量计数器 ÷ 时间，而不是只看时间；
- 把"随机"和"确定"分开 —— 同一现象跨多次独立运行是否复现。

**动手前先问：什么测量能让我在两个解释之间做出选择？**

---

## 5. 测量纪律（违反即白跑）

- A/B **同作业内交错**（ABBA 回文），基线与候选**分别构建成两个二进制**再比。
- **<2% 的效应：四对交错，判据是"候选是否全部高于基线"，不是均值差。**
  单点读数骗过人很多次。
- 成绩口径：交付脚本连跑多遍**取中位**；单次读数不作数。
- **探针必须能 FAIL**（故意错的实现要真的报错，证明它确实跳过了工作），
  探针数字**永不进成绩**。
- **变体优先用编译期开关，而不是代码拷贝或分支。**
  基线与候选只差一个宏、同源编译 → 差异被结构性限制成一个变量；
  且被否决的实现**默认关着留在主干上**——几天后一次编译就能重新定价。
  **那段"死代码"是资产，不是垃圾。**（分支上的变体会脱离主干继续演进，
  几天后再比必须先合并，比较立刻被污染。）
- 旋钮会互相踩（曾有两个分块参数不一致直接算错结果）：
  **任何结构性改动之后，全量重扫一遍旋钮。**

---

## 6. token 纪律

- 文档只 `grep -n` 定位 + `sed -n` 切片，**不整篇读**；作业输出只取汇总行。
- 写代码的重活交给便宜模型的 sub-agent，你负责**设计与判定**。
- handoff 必须是**层次化索引**，让下一个会话按需取，而不是从头读。
- **给负结果留预算**：一条被完整记录的负结果（带配方、带判据）是资产——
  它把"未知且吓人"变成"十分钟可复用"。

---

## 7. 会话收尾必须做的

1. 把"当前最优"冻结进 `submit/`（精英存档），并**独立复测**一遍。
2. 每条新结论按 §1 的格式落 `OPTIMIZATIONS.md` + `LEDGER.tsv`。
3. 更新 `CLAUDE.md` 的现状表。
4. **写下你这一轮"关掉"的门**，以及关它的理由属于哪种缺陷类型、将来该怎么检查。

---

## 8. 主动向用户要什么

**agent 做不了自己的外层循环，因为它的状态就是它自己的输出。** 以下四类主动要：

1. **机器/算法事实**——你不可能从代码里推出来的（例："这台机器 X 和 Y 可以同发"）。
   注意：**注入的事实会衰减**，如果一条事实你没在两轮内用上，它就沉底了，要主动回查。
2. **外部谱系**——有没有别人在做同一题？别人的结论能证伪你的收敛。
3. **结果审计**——请用户独立跑一次交付脚本。agent 不会自发怀疑自己的报数。
4. **预算与目标**——值不值得为 +2% 花一天。

**反面提醒**：当你听见自己说"我需要外部信息才能继续"时，先检查一遍——
**往往不是真的缺信息，是在给一次收敛找理由。**
trsm 里有一条线索是我自己写下"唯一还没打过的枪"，然后自己没打；
用户说"测一下"，四个作业就出结果了。
